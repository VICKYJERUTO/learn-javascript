for(let i=0 ;1<name.length; 1++){
 Text+= name[1] +"br";
}

while (i < 10) {
 text += "The number is " + i;
  i++;
  }

  const person = {fname:"John", lname:"Doe", age:25};

for (let x in person) {
  text += person[x];
}

let language = "JavaScript";

let text = "";
for (let x of language) {
text += x;
}
