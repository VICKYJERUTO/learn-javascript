//fuctions to compute the product of parameter1 and parameter2
function myfunction(parameter1,parameter2){
    return parameter1*parameter2;
   }
   //functions is called and finalized to return the results
   let x=myfunction(4,5);
   function myfunction(a,b){
   //function the product of a,b.
   return a*b;
   }